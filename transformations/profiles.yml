# dbt profiles configuration for local SQLite warehouse database
transformations:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: 'warehouse'
      schema: 'main'
      schemas_and_paths:
        main: "../data/warehouse.db"
      schema_directory: "../data"
  target: dev
